name: Deploy Backend to Lightsail

on:
  push:
    branches: [master]
    paths:
      - 'backend/**'

jobs:
  deploy-backend:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v3

      - name: 🦀 Setup Rust
        uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          target: x86_64-unknown-linux-musl  # ⚡ CAMBIO 1: gnu → musl
          override: true

      # ⚡ NUEVO: Instalar herramientas musl
      - name: 🔧 Install musl tools
        run: |
          sudo apt-get update
          sudo apt-get install -y musl-tools

      - name: 💾 Cache cargo dependencies
        uses: actions/cache@v3
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            backend/target
          key: ${{ runner.os }}-cargo-${{ hashFiles('backend/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-

      - name: 🏗️ Build Rust backend
        working-directory: ./backend
        run: |
          rustup target add x86_64-unknown-linux-musl  # ⚡ NUEVO: Agregar target
          cargo build --release --target x86_64-unknown-linux-musl  # ⚡ CAMBIO 2: especificar target

      - name: 📤 Upload Rust binary to Lightsail
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ubuntu
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 22
          source: "backend/target/x86_64-unknown-linux-musl/release/s3-parquet-explorer"  # ⚡ CAMBIO 3: ruta musl
          target: "/var/www/apps3browser/backend"
          strip_components: 4  # ⚡ CAMBIO 4: ahora son 4 niveles

      - name: 🔧 Setup and start Rust service
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ubuntu
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            chmod +x /var/www/apps3browser/backend/s3-parquet-explorer

            sudo tee /etc/systemd/system/apps3browser-backend.service > /dev/null << 'EOF'
            [Unit]
            Description=S3 Parquet Explorer Backend
            After=network.target

            [Service]
            Type=simple
            User=ubuntu
            WorkingDirectory=/var/www/apps3browser/backend
            ExecStart=/var/www/apps3browser/backend/s3-parquet-explorer
            Restart=always
            RestartSec=10
            MemoryMax=512M
            Environment="RUST_LOG=info"

            [Install]
            WantedBy=multi-user.target
            EOF

            sudo systemctl daemon-reload
            sudo systemctl enable apps3browser-backend
            sudo systemctl restart apps3browser-backend

            sleep 3
            echo "📊 Service status:"
            sudo systemctl status apps3browser-backend --no-pager

            echo ""
            echo "✅ Deployment completed at $(date)"
            echo "📍 Backend: /var/www/apps3browser/backend/s3-parquet-explorer"

      - name: ✅ Backend deployment summary
        run: |
          echo "========================================="
          echo "🎉 Backend deployed successfully!"
          echo "========================================="
          echo "🦀 Backend: /var/www/apps3browser/backend/s3-parquet-explorer"
          echo "🔧 Binary type: Static (MUSL) - Compatible with Ubuntu 22.04"
          echo "========================================="